<h1>Venues</h1>

<section>
    <% @venue.each do |venue| %>
        <p><%= link_to venue.title, venue_path(venue) %></p>
    <% end %>    
</section>

<p>
    <%= link_to "Add Venue" , new_venue_path %>
</p>

